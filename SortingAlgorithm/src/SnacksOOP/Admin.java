package SnacksOOP;

public class Admin extends EStoreUsers{
    public Admin(int age, String name, String emailAddress, String phoneNumber, String password, String homeAddress) {
        super(age, name, emailAddress, phoneNumber, password, homeAddress);
    }
}
